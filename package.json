{
  "name": "moorstead",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "A procedural sandbox game set on the North York Moors",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify": "node scripts/verify-rail.mjs && node scripts/verify-rail-clearance.mjs && node scripts/verify-train-view.mjs",
    "verify:rail": "node scripts/verify-rail.mjs",
    "verify:clearance": "node scripts/verify-rail-clearance.mjs",
    "verify:train": "node scripts/verify-train-view.mjs"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "vite": "^5.3.0"
  }
}
